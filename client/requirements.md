## Packages
zustand | State management for product comparison
framer-motion | Page transitions and sleek hover animations
lucide-react | Elegant icons for e-commerce (cart, wishlist, user)

## Notes
- Using Replit Auth via `/api/login` and `/api/logout`
- The `useAuth` hook is imported from `@/hooks/use-auth`
- Compare store uses Zustand to hold up to 3 products
- The theme uses a clean white background with soft warm gray and gold accents to match the logo and provide a premium feel
