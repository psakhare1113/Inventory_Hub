/*
package com.pixelbloom.shipping.clients;

import com.pixelbloom.shipping.dto.CustomerAddress;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

@FeignClient("orders-service")
public interface OrdersClient {
    @GetMapping("/api/orders/address/{customerId}")
    CustomerAddress getCustomerAddress(@PathVariable Long customerId);
}
*/
