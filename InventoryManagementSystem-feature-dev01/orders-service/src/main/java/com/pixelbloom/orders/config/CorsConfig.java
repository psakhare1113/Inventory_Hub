package com.pixelbloom.orders.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS is handled by API Gateway - no configuration needed here
    // This class is intentionally empty to avoid duplicate CORS headers
}